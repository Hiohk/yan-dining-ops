# ⚠️ 正常情况下**不用手填这个文件** —— 跑一次引导脚本，它会建好工作区、签发 Key
#    并直接写进 apps/api/.env（0600 权限）：
#
#      OPENHEX_API_KEY=mysta_... pnpm --filter @lto/api bootstrap -- --slug=your-workspace
#
#    下面是各项的含义和手动路径，供脚本跑不了、或想自己接管时参考。
#    真实 .env 已在 .gitignore 里（.env / .env.*），不要提交。

# ⚠️ 这两个都是**后端密钥**，只能待在这个进程里。
#    绝不能出现在前端代码、环境变量前缀（VITE_/NEXT_PUBLIC_）或任何构建产物里。

# 工作区 API Key（形如 sk_ + 64 位十六进制）。
# 注意：不是 mysta_ 开头的个人 API Key —— 签发会话令牌只接受工作区 Key，
#      而工作区 Key 只能**用个人 Key 签发**（自己签不了自己），这是两把不同的钥匙。
# 手动获取（脚本内部就是这三步，用个人 Key 调）：
#   ① GET  /api/v2/workspaces                 拿 slug（列表为空则先 POST 建一个）
#   ② POST /api/v2/workspaces/{slug}/api-keys 签发 → token **只在这一次返回**，立刻存好
OPENHEX_WORKSPACE_KEY=sk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 工作区 slug（工作区在平台上的短标识）。
# 方案 A 靠它定位「你的成员与计费边界」—— 访客在这里成为成员，拿着你签的令牌去对话。
# 必填，没有它 startVisitorSession 无处落脚。
OPENHEX_WORKSPACE_SLUG=your-workspace-slug

# 可选：网关监听端口。默认 8000，与 apps/web/vite.config.ts 的 /api 代理目标一致，
# 改了端口记得同步改代理配置。
# PORT=8000

# 可选：监听地址。默认 127.0.0.1 —— 本地开发刻意只听回环，只有 vite 代理能到。
# Docker 里由 docker-compose.yml 注入 HOST=0.0.0.0（容器内的 127.0.0.1 是它自己的
# 回环，同网络的 nginx 容器连不上，现象是 502）。**本地不要设成 0.0.0.0**，
# 那会让同一个局域网里任何人都能直接打「用工作区 Key 换会话令牌」这个端点。
# HOST=127.0.0.1

# 可选：覆盖平台 API 地址。默认 https://api.openhex.tech，只在自建 / 预发环境需要。
# ⚠️ 凭据跟着这个地址走 —— 指向哪，工作区 Key 就交给谁。别指向你不信任的域名。
# OPENHEX_API_BASE=https://api.openhex.tech
