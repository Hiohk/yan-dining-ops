# 复制为 apps/web/.env 后填写。真实 .env 已在 .gitignore 里，不要提交。
#
# ⚠️ 这里的东西**全部会进构建产物**，任何人都能从浏览器里读到。
#    只有 Agent ID 这类非密钥可以放；API Key（mysta_… / sk_…）永远不要写进来。

# OpenHex Agent ID —— 首页点开 Agent，地址栏 /console/ 后面那一段 UUID。
# 它不是密钥：知道它只能「向这个 Agent 发消息」，拿不到你的账号或工作区权限。
VITE_OPENHEX_AGENT_ID=00000000-0000-0000-0000-000000000000
